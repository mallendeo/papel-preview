{
  "name": "papel-preview",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "parcel src/index.html -p 3001",
    "parcel": "parcel build src/index.html --public-url ./",
    "build": "rimraf dist && npm run parcel && cp -r src/static/* dist/"
  },
  "devDependencies": {
    "parcel-bundler": "^1.8.1",
    "rimraf": "^2.6.2"
  }
}
